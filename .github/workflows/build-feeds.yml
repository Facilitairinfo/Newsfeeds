name: Build Feeds

on:
  workflow_dispatch:
  schedule:
    - cron: "0 */6 * * *" # elke 6 uur
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Install dependencies + feedparser
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt
          pip install feedparser

      # --- Events ---
      - name: Build events feed
        run: python scripts/build-feed.py sites-events.yml

      - name: Validate events RSS
        run: |
          python -c "import feedparser; d=feedparser.parse(open('docs/events.xml','rb').read()); \
          assert not d.bozo, f'Events RSS parse error: {d.bozo_exception}'"

      # --- Podcast ---
      - name: Build podcast feed
        run: python scripts/build-feed.py sites-podcast.yml

      - name: Validate podcast RSS
        run: |
          python -c "import feedparser; d=feedparser.parse(open('docs/podcast.xml','rb').read()); \
          assert not d.bozo, f'Podcast RSS parse error: {d.bozo_exception}'"

      # --- FMN Nieuws ---
      - name: Build FMN nieuws feed
        run: python scripts/build-feed.py sites-fmn-nieuws.yml

      - name: Validate FMN nieuws RSS
        run: |
          python -c "import feedparser; d=feedparser.parse(open('docs/fmn-nieuws.xml','rb').read()); \
          assert not d.bozo, f'FMN nieuws RSS parse error: {d.bozo_exception}'"

      # --- Gecombineerd ---
      - name: Build gecombineerde feed
        run: python scripts/build-feed.py sites-events.yml sites-podcast.yml sites-fmn-nieuws.yml

      - name: Validate gecombineerde RSS
        run: |
          python -c "import feedparser; d=feedparser.parse(open('docs/combined.xml','rb').read()); \
          assert not d.bozo, f'Combined RSS parse error: {d.bozo_exception}'"

      - name: Commit and push changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add docs/*.xml
          git commit -m "Auto-update feeds" || echo "Geen wijzigingen"
          git push
